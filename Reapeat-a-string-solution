function repeatStringNumTimes (str, num) {

  if(num <= 0 ){
    return ""
  }

  let repeatStr = ""
  for  (let i = 1; i <= num; i++ ){
    repeatStr += str
  }
  
  return repeatStr
}

module.exports = repeatStringNumTimes
